pygame-ce>=2.5
numpy>=2.0
PyOpenGL>=3.1
Pillow>=10.0
segno>=1.6  # pure-Python QR for the secret-local companion lobby (optional: falls back to text)
# Optional: MP4 replay export (bundles a static ffmpeg). GIF works without it.
# pip install imageio-ffmpeg
